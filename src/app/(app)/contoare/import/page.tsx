import ImportContoareClient from "./ImportContoareClient";

export const metadata = { title: "Import citiri contoare — SpokAdmin" };

export default function ImportContoarePage() {
  return <ImportContoareClient />;
}
